# リポジトリ内ファイルへの絶対パス記載禁止ルール

## ルール

コミット対象のファイル（ドキュメント、設定ファイル、コードのコメント等）に
ユーザー名を含む絶対パス（環境依存パス）を記載してはならない。

**禁止例**:

```text
/Users/johndoe/.claude/plans/my-plan.md
/home/johndoe/workspace/project/...
C:\Users\johndoe\...
```

**許可例**:

```text
.claude/skills/md-to-medical-html/SKILL.md      （リポジトリルートからの相対パス）
Types-of-headache/html-files/Headaches/Migraine.html   （リポジトリ内ファイルの相対指定）
```

## 適用対象

- `PROGRESS.md` などのドキュメント
- `.claude/rules/` 内のファイル
- 各種 `.skills/` 内の `SKILL.md` や関連スクリプトファイル
- 各種 Python/JavaScript スクリプト内のコメントおよびハードコードされたパス
- HTML / Markdown 内のファイル参照やリンク

## 外部ファイルを参照したい場合

リポジトリ外ファイルやローカル固有ファイルをコミット対象のドキュメントから
参照したい場合は、絶対パス（`file:///Users/...` など）を絶対に記載せず、
テキストとしてリポジトリ相対パスで表現するか、必要なファイルを先にリポジトリ内に配置した上で
相対リンクを使用する。

## 理由

絶対パスには OS ユーザー名が含まれ、パブリックリポジトリや
チームへのプッシュ時に個人情報を晒すリスクがある。

## AI エージェント向けの検証手順（必須）

AI エージェントは、`git commit` などのコミットを行う前に、必ずステージングされた変更差分（`git diff --cached`）に対して、ローカル絶対パスやユーザー名が含まれていないかを機械的に確認しなければなりません。

具体的には、コミット前に以下の検証用の検索コマンドを自律的に実行し、PII が混入していないことを検証してください。

```bash
# コミット対象の差分にローカル絶対パス（Users/ や home/）が含まれていないかチェック
# プレースホルダー（johndoe）を除く絶対パスが検出された場合はコミットを中止する
git diff --cached | grep -E '^\+[^+]' | grep -E '(/Users/|/home/|C:\\Users\\)' | grep -vE 'johndoe'
```

このチェックで結果（追加行）が出力された場合は、該当箇所を削除または相対パスに変更し、クリーンであることを確認した上でコミットを実行してください。
